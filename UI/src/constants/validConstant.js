export const regexValidation = {
  SSNA: /^[A-Za-z0-9]+$/, // Space and Special Characters Not allowed
  PNOA: /^[0-9]+$/, // Phone number format
  NCOA: /^[0-9,]+$/, // Number and comma (,) only Allowed
  NUMA: /^[0-9]+$/, // Number only Allowed
};
